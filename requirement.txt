Flask==2.0.2
Werkzeug==2.0.2
requests==2.31.0
fitz==0.0.1.dev2  # For working with PDFs (PyMuPDF wrapper)
opencv-python==4.5.3.56  # For image processing (cv2)
PaddleOCR==2.0.6  # For OCR processing (paddleocr)
torch==2.0.0  # For SAM model processing (from sam_utils.py)
